<div class="msg msg-recieved">
  <strong><%= message.user.username %>:</strong>
  <span> <%= message.body %> </span>
  <div class="msg-status"><span> <%= message.created_at.strftime("%H:%M") %> </span></div>
</div>